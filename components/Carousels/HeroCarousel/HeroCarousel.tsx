import { useState, useEffect, useRef } from "react"
const HeroCarousel = () => {
  return (
    <div className="w-[500%] min-h-[calc(100vh-142px)] bg-blue-500 flex">
        Hero Carousel

      </div>
  )
}

export default HeroCarousel