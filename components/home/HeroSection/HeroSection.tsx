import HeroCarousel from "@/components/Carousels/HeroCarousel/HeroCarousel";

const HeroSection = () => {
  return (
    <div className="bg-gray-300 w-full min-h-[calc(100vh-142px)] overflow-hidden">
     <HeroCarousel/>
    </div>
  );
};

export default HeroSection;
